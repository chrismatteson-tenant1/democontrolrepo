---
:backends:
  - yaml

:hierarchy:
  - "clientcert/%{clientcert}"
  - "environment/%{environment}"
  - "datacenter/%{datacenter}"
  - "virtual/%{virtual}"
  - "%{testkey}"
  - "common"

:yaml:
  :datadir: "/etc/puppetlabs/puppet/environments/production/data"
